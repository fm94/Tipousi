#include "activation/softmax.hpp"

namespace Tipousi
{
    namespace Activation
    {
        Softmax::Softmax()
        {
        }

        void Softmax::forward(const Eigen::MatrixXf &in, Eigen::MatrixXf &out)
        {
        }
        void Softmax::backward(const Eigen::MatrixXf &dout, Eigen::MatrixXf &ddout)
        {
        }
    };
};
